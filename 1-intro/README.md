These exercises where solved using http-server, a simple zero-configuration command-line http server. To install it, run the following command:

```bash
npm install http-server
```

running the server:

```bash
npx http-server oef1/
```
